import _ from 'lodash';

const test = {
    foo: "bar",
    events: {
        fee: 1,
        fi: 2,
        foe: [{gee: 3, wiz: 4}]
    }
}

const newObj = {
    foo: "bar",
    events: {
        fee: 1,
        fi: 2,
        foe: [{gee: 3, wiz: 4}]
    }
}

// const updateObj = function (oldObj, newObj) {
//     return 
// }
